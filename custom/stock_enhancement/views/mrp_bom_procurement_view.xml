<?xml version="1.0"?>
<odoo>
  <record id="mrp_bom_procurement_form_action" model="ir.actions.act_window">
        <field name="name">mrp.bom.procurement</field>
        <field name="res_model">mrp.bom.procurement</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_available': True}</field>
  </record>

  <record id="mrp_bom_procurement_tree_view" model="ir.ui.view">
    <field name="name">mrp.bom.procurement.tree</field>
    <field name="model">mrp.bom.procurement</field>
    <field name="arch" type="xml">
        <tree string="Bill of Materials" sample="1">
            <field name="active" invisible="1"/>
            <field name="sequence" widget="handle"/>
            <field name="product_tmpl_id"/>
            <field name="code" optional="show"/>
            <field name="type"/>
            <field name="product_id" groups="product.group_product_variant" optional="hide"/>
            <field name="company_id" groups="base.group_multi_company" optional="show"/>
            <field name="product_qty" optional="hide"/>
            <field name="product_uom_id" groups="uom.group_uom" optional="hide" string="Unit of Measure"/>
        </tree>
    </field>
  </record>




</odoo>
